kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "image-selenium-os"
spec:
  source:
    type: Git
    git:
      uri: "https://github.com/leprechaun/selenium-on-openshift"
  strategy:
    type: Docker
    dockerStragegy:
      noCache: true
  output:
    to:
      kind: "ImageStreamTag"
      name: "leprechaun-selenium-os:latest"
